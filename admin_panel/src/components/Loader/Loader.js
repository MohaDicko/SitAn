import React from 'react';

// project import
import Progress from './Progress';

// ==============================|| LOADER ||============================== //

const Loader = () => {
  return (
    <React.Fragment>
      Loading...
    </React.Fragment>
  );
};

export default Loader;
